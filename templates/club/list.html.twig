<h1> la list des formations est :</h1>
{% set somme = 0 %}
<table border="3">
    <tr>
        <th>Referance</th>
        <th>Titre</th>
        <th>Description</th>
        <th>Nbr</th>
        <th>Detail</th>
    </tr>
    {% for i in formations %}
    {#{% if 'theorique' in i.Description %}#}
    <tr>
        <td>{{i.ref}}</td>
        <td>{{i.Titre|upper}}</td>
        <td>{{i.Description}}</td>
        <td>{{i.nb_participants}}</td>
        <td><form action = "{{path('detailF',{'titre':i.Titre}) }}">    {# creation button#}
         <button>Participer</button></form>
         <a href="{{path('detailF', {'titre':i.Titre}) }}">Participer </a> </td>
    </tr>
    {% set somme = somme + i.nb_participants %}
    {#{% endif %}#}
    {% endfor %}
</table>
<h2> le nombres des formations est {{formations|length}} </h3>
{% set maxv = 0 %}
{% for j in formations %}
{% set maxv = max(maxv,j.nb_participants) %}
{% if j.nb_participants == maxv %}
    <h2>la formation ayant le plus grand nombre de participants est  {{j.Titre|upper}} </h2>
{% endif %}
{% endfor %}
</table>
<h2> le nombres total des participants est {{somme}} </h3>